{
  "name": "web-test",
  "version": "1.0.0",
  "description": "dung de test web",
  "main": "bootstrap.min.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ntluan",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-sass": "^4.0.2"
  },
  "dependencies": {
    "gulp-livereload": "^4.0.0"
  }
}
